class TestCase::R {
  use R;
  use R::NDArray;
  use R::NDArray::Int;
  use R::NDArray::Long;
  use R::NDArray::Float;
  use R::NDArray::Double;
  use R::NDArray::DoubleComplex;
  use R::NDArray::String;
  
  use R::Operation;
  use R::Operation::Int;
  
  static method test : int () {
    
    return 1;
  }
}
