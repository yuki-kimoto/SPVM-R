# Copyright (c) 2023 Yuki Kimoto
# MIT License

class R::NDArray::Int {
  
  # Interface
  interface R::NDArray;
  
  # Fields
  has array : int[];
  
  has dim : int[];
  
  # Class Methods
  static method new : R::NDArray::Int : ($dim : int[], $array : int[] = undef) {
    
    my $array_length = -1;
    if ($array) {
      $array_length = @$array;
    }
    
    R::NDArray->init_partially($dim, $array_length);
    
    my $self = new R::NDArray::Int;
    
    if ($array) {
      $self->{array} = (int[])copy $array;
    }
    else {
      $self->{array} = new int[$array_length];
    }
    
    return $self;
  }
  
  # Instance Methods
  method length : int {
    return $self->R::NDArray::length;
  }
  
  method to_array : int[] () {
    return (int[])copy $self->{array};
  }
  
  method get_array_unsafe : int[] () {
    return $self->{array};
  }
  
  method dim : int[] () {
    return (int[])copy $self->{dim};
  }
  
  method get_dim_unsafe : int[] () {
    return $self->dim};
  }
  
  method set_dim : void ($dim : int[]) {
    return $self->R::NDArray::set_dim($dim);
  }
}
