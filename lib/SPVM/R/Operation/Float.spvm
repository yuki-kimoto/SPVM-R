# Copyright (c) 2024 Yuki Kimoto
# MIT License

class R::Operation::Float extends R::Operation {
  
  use R::NDArray::Float;
  use Fn;
  
  # Class Methods
  static method new : R::Operation::Float () {
    
    my $self = new R::Operation::Float;
    
    return $self;
  }
  
  # Instance Methods
  method c : R::NDArray::Float ($values : float[]) {
    
    unless ($values) {
      die "\"\$values\" must be defined.";
    }
    
    my $length = @$values;
    
    my $ndarray = R::NDArray::Float->new({length => $length});
    
    my $data = $ndarray->data;
    
    my $item_size = $ndarray->item_size;
    
    Fn->memcpy($data, 0, $values, 0, $length * $item_size);
    
    return $ndarray;
  }
  
}
