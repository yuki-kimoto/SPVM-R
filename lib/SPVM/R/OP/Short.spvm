# Copyright (c) 2024 Yuki Kimoto
# MIT License

class R::OP::Short {
  
  use R::NDArray::Short;
  use ShortList;
  use R::OP;
  
  # Class Methods
  static method c : R::NDArray::Short ($x_data : short[], $x_dim : int[] = undef) {
    
    my $x_ndarray = R::NDArray::Short->new({dim => $x_dim, data => $x_data});
    
    return $x_ndarray;
  }
  
}
